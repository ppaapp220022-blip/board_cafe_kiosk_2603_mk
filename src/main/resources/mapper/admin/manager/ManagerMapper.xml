<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.board_cafe_kiosk_2603.mapper.admin.manager.ManagerMapper">

    <!-- 로그인 ID로 단건 조회 -->
    <select id="findByLoginId" resultType="org.example.board_cafe_kiosk_2603.domain.admin.manager.Manager">
        SELECT id, login_id, password, name, role, is_active, created_at
        FROM manager
        WHERE login_id = #{loginId}
    </select>

    <!-- 직원 등록 -->
    <insert id="insert">
        INSERT INTO manager (login_id, password, name, role, is_active)
        VALUES (#{loginId}, #{password}, #{name}, #{role}, #{isActive})
    </insert>

    <!-- 활성/비활성 토글 -->
    <update id="updateActive">
        UPDATE manager
        SET is_active = #{isActive}
        WHERE id = #{id}
    </update>

    <!-- 내 정보 수정 (이름, 비밀번호 업데이트) -->
    <update id="updateProfileInfo">
        UPDATE manager
        SET name = #{name},
            password = #{password}
        WHERE login_id = #{loginId}
    </update>

    <!--===================페이징====================-->
    <!-- 페이징 목록 조회 -->
    <select id="selectList" resultType="org.example.board_cafe_kiosk_2603.domain.admin.manager.Manager">
        SELECT id, login_id, password, name, role, is_active, created_at
        FROM manager
        WHERE 1=1
        <if test='filter != null and filter == "active"'>
            AND is_active = true
        </if>
        <if test='filter != null and filter == "inactive"'>
            AND is_active = false
        </if>
        <if test="keyword != null and keyword != ''">
            AND (name LIKE CONCAT('%', #{keyword}, '%')
            OR login_id LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY is_active DESC, created_at DESC
        LIMIT #{skip}, #{size}
    </select>

    <!-- 페이징 전체 개수 조회 -->
    <select id="selectCount" resultType="int">
        SELECT COUNT(*)
        FROM manager
        WHERE 1=1
        <if test='filter != null and filter == "active"'>
            AND is_active = true
        </if>
        <if test='filter != null and filter == "inactive"'>
            AND is_active = false
        </if>
        <if test="keyword != null and keyword != ''">
            AND (name LIKE CONCAT('%', #{keyword}, '%')
            OR login_id LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </select>

</mapper>