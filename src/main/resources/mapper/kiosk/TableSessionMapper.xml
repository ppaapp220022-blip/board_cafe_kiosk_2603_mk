<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.board_cafe_kiosk_2603.mapper.kiosk.TableSessionKioskMapper">
    <!-- 세션 생성 (패키지 선택 완료 시) -->
    <insert id="insertSession" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO board_cafe_kiosk_2603.table_session
        (table_id, package_id, initial_guest_cnt, is_active, total_amount)
        VALUES (#{tableId}, #{packageId}, #{initialGuestCnt}, TRUE, 0)
    </insert>
</mapper>